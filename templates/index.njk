{% extends "./base.njk" %}

{% block body %}
  <section>
    <header>
      <h1>Egovelox Blog</h1>
    </header>

    <ul>
    {% for post in posts %}
      <li>
        <a href="{{ post.slug }}">{{ post.title }}</a>
      </li>
    {% endfor %}
    </ul>

  </section>
{% endblock %}